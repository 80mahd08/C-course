#include <stdio.h>
#include <stdbool.h>

void main()
{
    int age = 20;
    double moy = 11.64;
    char firstLetterInName = 'M';
    char name[] = "Mehdi";
    //if you wont to use boolean data type you have to include stdbool.h
    bool isMarried = false;
}

// ? what is the diffrence between float and double
// => double is more precise than float