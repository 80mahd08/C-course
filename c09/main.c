#include <stdio.h>

void main()
{
    double n1;
    double n2;
    printf("Enter the first number: ");scanf("%lf", &n1);
    printf("Enter the second number: ");scanf("%lf", &n2);
    printf("The sum is %lf\n", n1 + n2);
}